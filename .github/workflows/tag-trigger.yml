name: Build on Tag

on:
  push:
    tags:
        - 'v1.[0-9]'  # Accepte v1.0, v1.1, v1.2, jusqu'à v1.9

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Build project
        run: echo "Building project for tag ${{ github.ref_name }}"
